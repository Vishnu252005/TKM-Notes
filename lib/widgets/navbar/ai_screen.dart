import 'package:flutter/material.dart';

class AIScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI')),
      body: Center(child: Text('AI Screen')),
    );
  }
} 